def sum_to_n(n):
    """
    Функция для вычисления суммы чисел от 1 до n.
    
    :param n: Целое число, до которого будет производиться сумма.
    :return: Сумма чисел от 1 до n.
    """
    if n < 1:
        return 0
    return sum(range(1, n + 1))

# Пример использования:
N = 10
result = sum_to_n(N)
print(f"Сумма чисел от 1 до {N} равна {result}.")
